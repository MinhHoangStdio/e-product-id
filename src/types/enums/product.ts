export enum EApprovalStatus {
  Approve = "approved",
  Reject = "reject",
  Pending = "pending",
  Requesting = "requesting",
  Ban = "banned",
}

export enum EApprovalRequest {
  Approve = "approved",
  Reject = "reject",
  Ban = "banned",
}
